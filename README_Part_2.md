# Python Fundamentals — Part 2

A structured continuation of my Python fundamentals revision, focused on **Conditional Statements, Loops, and Functions**. This part builds programming logic through explanations, syntax, examples, and practice problems.

## 📚 Topics Covered

### 1. Conditional Statements
- `if`
- `elif`
- `else`
- Logical operators in conditions
- Nested conditional statements
- Ternary / Conditional expressions
- `match-case`

### 2. Practice Problems — Conditionals
- Checking multiples of 5
- Checking odd and even numbers

### 3. Loops
- Introduction to loops
- `while` loop
- `for` loop
- Nested loops
- Loop control statements
  - `break`
  - `continue`
- Using `in` with loops and membership checking
- Iterating over strings
- Counting characters

### 4. `range()` Function
- `range(stop)`
- `range(start, stop)`
- `range(start, stop, step)`
- Inclusive and exclusive boundaries

### 5. Functions
- Defining functions with `def`
- Calling functions
- Parameters and arguments
- `return` keyword
- Positional arguments
- Keyword arguments
- Default parameters
- Variable-length positional arguments (`*args`)
- Variable-length keyword arguments (`**kwargs`)

### 6. Built-in Functions
Examples of commonly used Python built-in functions:
- `print()`
- `input()`
- `len()`
- `type()`
- `sum()`
- `max()`
- `min()`
- `range()`
- `abs()`

### 7. Lambda Functions
- `lambda` syntax
- Anonymous functions
- Single-expression functions
- Implicit return

### 8. Recursion
- Recursive functions
- Base case
- Recursive case
- Factorial using recursion

### 9. Practice & Problem Solving
- Factorial of a number
- Finding the largest of three numbers
- Condition-based problems
- Loop-based problems

---

## 🎯 Purpose

This notebook is part of my **complete Python revision**, where I am rebuilding my Python knowledge from the fundamentals and documenting concepts for long-term revision.

The focus of Part 2 is developing a strong understanding of **program control flow, iteration, functions, and problem-solving logic**.

## 🛠️ Tools

- Python
- Jupyter Notebook

## 📌 Status

**Part 2 — Python Fundamentals: Completed**

More Python fundamentals, data structures, advanced concepts, and OOP will be covered in the upcoming parts.
